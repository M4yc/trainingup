import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 4,
    alignItems: 'center',
    gap: 12
  },
  nameExe: {
    color: 'white',
    fontSize: 16,
    fontWeight: '500'
  },
  containerInner: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4
  }
});

export default styles;
